"""
Plantilla de configuración local de la API key de Groq. Cópiala como
`local_config.py` (en esta misma carpeta) si clonaste el repositorio con
git — ese archivo copiado NO se sube a git (ver .gitignore) y NO se pide
ni se muestra en ningún campo de la interfaz gráfica: se edita
directamente, una sola vez. (Si en cambio descargaste el .rar de Vortex,
`local_config.py` ya viene incluido y listo para editar — no hace falta
copiar esta plantilla.)

Consigue una API key gratuita en https://console.groq.com/keys, pégala
entre las comillas de abajo (en tu copia local_config.py, no aquí), y
guarda el archivo. Vortex la usará automáticamente la próxima vez que
abras la aplicación o vuelvas a presionar "Analizar resultados con IA" —
no hay que reiniciar nada más.
"""

GROQ_API_KEY = ""
